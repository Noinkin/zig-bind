## API Report File for "zig-bind"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export class ZigBindRegistry {
    constructor(wasmInstance: WebAssembly.Instance);
    alloc<T extends ZigBindType>(type: T, source: number | number[] | Float32Array | Uint32Array | Uint8Array): ZigVector<T>;
    bind(functionName: string): (...args: any[]) => any;
    // (undocumented)
    memory: WebAssembly.Memory;
    // (undocumented)
    reset(): void;
}

// @public (undocumented)
export type ZigBindType = 'f32' | 'u32' | 'u8';

// @public (undocumented)
export type ZigVector<T extends ZigBindType> = (T extends 'f32' ? Float32Array : T extends 'u32' ? Uint32Array : Uint8Array) & {
    ptr: number;
};

// (No @packageDocumentation comment for this package)

```
